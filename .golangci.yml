run:
  timeout: 5m
  tests: true

linters:
  enable:
    - govet
    - staticcheck
    - unused
    - misspell
  disable:
    - errcheck
    - revive
    - ineffassign
    - gosec

linters-settings:
  govet:
    enable-all: true
    
  misspell:
    locale: US

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - govet
    - path: cmd/
      linters:
        - govet
